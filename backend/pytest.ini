[pytest]
markers =
    integration: exercises real Mongo via testcontainers; requires docker
# Exclude the integration suite from the default run. Opt in with:
#   pytest -m integration tests/integration
# CI schedules a dedicated job for it.
addopts = --ignore=tests/integration
